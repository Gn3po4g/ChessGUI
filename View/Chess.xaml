<UserControl x:Class="ChessGUI.View.Chess"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ChessGUI.ViewModel"
             xmlns:local="clr-namespace:ChessGUI"
             d:DataContext="{d:DesignInstance Type=vm:Chess}"
             mc:Ignorable="d"
             x:Name="ChessView">
    <UserControl.Resources>
        <Style x:Key="Style" TargetType="Image">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Focused}" Value="true">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Width="{x:Static vm:Board.ChessSize}" Height="{x:Static vm:Board.ChessSize}">
        <Grid.Background>
            <ImageBrush ImageSource="{Binding ImageSource}"/>
        </Grid.Background>
        <Grid.InputBindings>
            <MouseBinding Gesture="LeftClick"/>
        </Grid.InputBindings>
        <!-- <Image IsHitTestVisible="False" /> -->
        <!-- <Image Style="{StaticResource Style}" -->
        <!--        Source="{StaticResource Mark}" /> -->
    </Grid>
</UserControl>